{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "Settings": {
    "SignToolPath": "C:\\Program Files (x86)\\Microsoft Visual Studio\\Shared\\NuGetPackages\\microsoft.windows.sdk.buildtools\\10.0.26100.1742\\bin\\10.0.26100.0\\x86\\signtool.exe",
    "SignToolOptions": "sign /tr \"http://timestamp.digicert.com\" /td SHA256 /fd SHA256 /f \"{BaseDirectory}testcert.pfx\" /p \"password\" /v \"{FilePath}\"",
    "SigntoolTimeoutInMs": 15000,
    "MaxRequestBodySize": 1000000000,
    "SemaphoreSlimProcessPerCpuLimit": 1,
    "HealthCheckCacheInMs": 30000,
    "HealthCheckQueueStallInMs": 60000,
    "OpenSSL": {
      "OpenSslPath": "openssl",
      "OpenSslOptions": "cms -sign -in \"{FilePath}\" -signer \"{WorkingDirectory}public_cert.crt\" -inkey \"{WorkingDirectory}private.key\" -keyform P12 -passin pass:password -out \"{FilePath}.sig\" -outform DER -md sha256 -binary",
      "OpenSslTimeoutInMs": 30000,
      "OsslSignCodePath": "osslsigncode",
      "TimestampOptions": "add -t \"http://timestamp.digicert.com\" -in \"{FilePath}.sig\" -out \"{FilePath}.timestamped.sig\"",
      // Public signer certificate to inspect for the /admin/status dashboard. Leave empty to skip.
      "SignerCertPath": ""
    },
    // Certificate to inspect for the /admin/status dashboard. Leave empty to skip cert reporting.
    "CertificatePath": "",
    "CertificatePassword": "",
    "CertificateWarningDays": 30
  },
  // If the JWT section is missing or the secret is not set then JWT validation will be disabled.
  "JWT": {
    "ValidAudience": "https://localhost",
    "ValidIssuer": "https://localhost",
    "Secret": "PLACEHOLDER",
    "PolicyName": "CodeSignPolicy"
  }
}
